[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=machineLearning.binning_kbins --cov-report=term-missing --cov-fail-under=90"

[tool.coverage.run]
branch = true

[tool.coverage.report]
show_missing = true
